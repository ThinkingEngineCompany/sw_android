<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Beauty" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Beauty" parent="Base.Theme.Beauty" >
        <item name="android:windowBackground">#ffffff</item>
    </style>
    <style name="Theme.BeautyS" parent="Base.Theme.Beauty" >
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="UnityThemeSelector" parent="Theme.Beauty">
        <!--设置顶部状态栏是否为透明-->
        <item name="android:windowTranslucentStatus">false</item>

        <!--Android 5.x开始需要把颜色设置透明，否则顶部导航栏会呈现系统默认的浅灰色-->
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!--设置顶部状态栏和底部导航栏是否为透明-->
        <item name="android:windowTranslucentNavigation">true</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="android:windowFullscreen">true</item>

        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>


    </style>
</resources>